class electronic:
    def __init__(self, id, price, model):
        self.id = int(id)
        self.price = price
        self.model = model  # we need to check if we need cast to int the id (int)supplier_id
